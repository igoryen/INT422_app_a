@model IEnumerable<AppMemory.Models.Student>

@{
    ViewBag.Title = "Ddl";
}

<h2>Ddl</h2>
@using (Html.BeginForm("Details", "Home"))
{
    <div>
        <div class="form-group">
            <div class="col-md-offset-2 col-md-10">
                @Html.DropDownList("li")
            </div>
        </div>
        <div class="form-group">
            <div class="col-md-offset-2 col-md-10">
                <input type="submit" value="Details" class="btn btn-default" />
            </div>
        </div>
    </div>
}
